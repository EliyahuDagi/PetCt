# AUTO-GENERATED by scripts/gen_ae2d_ablation_configs.py -- do not edit.
# ae2d reconstruction ablation point: latent_channels=16.
seed: 42
output_dir: outputs/ae2d_ablation/lc16
val_fraction: 0.2
test_fraction: 0.1
slice_manifest: data/training/slices.jsonl
batch_size: 16
num_workers: 4
learning_rate: 0.0001
max_epochs: 100
latent_channels: 16
model:
  spatial_dims: 2
  in_channels: 1
  out_channels: 1
  block_out_channels:
  - 32
  - 64
  - 128
  num_res_blocks: 2
loss:
  recon_l1_weight: 1.0
  kl_weight: 1.0e-06
perceptual_weight: 0.1
perceptual_backend: vgg
augment:
  enabled: true
  flip_prob: 0.5
  rot90_prob: 0.5
  affine_prob: 0.3
  affine_rotate_deg: 5.0
  affine_scale: 0.05
  affine_translate: 0.02
